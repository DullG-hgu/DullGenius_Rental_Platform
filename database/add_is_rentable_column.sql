-- =========================================================================
-- 추가 설명: 게임 대여 가능 여부 (열람 전용, 파손, 분실 등)를 위한 속성 추가
-- =========================================================================

-- 1. games 테이블에 대여 가능 여부 컬럼 추가 (기본적으로 모두 대여 가능하도록 설정)
ALTER TABLE public.games ADD COLUMN IF NOT EXISTS is_rentable BOOLEAN DEFAULT TRUE;

-- 2. 역할을 위한 주석 추가
COMMENT ON COLUMN public.games.is_rentable IS '게임 대여 가능 여부 (false일 경우 상세페이지에서 대여 불가 또는 열람 전용 상태로 전환됨)';
